# ici c'est le module principale
# on fait appelle a notre module d'interface graphique 
from ui import interface_gradio

# le module principale main nous sert juste a lancer notre appli

interface_gradio.lancement_interface()
